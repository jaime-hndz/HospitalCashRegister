@model CashRegister

@{
    ViewData["Title"] = "Finalizar día";
}

<h2>Cerrar día</h2>

<h3>¿Estás seguro que quieres finalizar el día actual?</h3>
<div>
    <form asp-action="End">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Finalizar" class="btn btn-danger" />
        <a asp-action="Index" class="btn">Cancelar</a>
        @*
        <a asp-action="PrintCashRegister" asp-route-id="@Model.Id">Imprimir</a>
        *@
    </form>
</div>